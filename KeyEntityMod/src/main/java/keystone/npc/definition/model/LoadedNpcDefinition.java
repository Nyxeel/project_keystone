package keystone.npc.definition.model;

/*
 * LoadedNpcDefinition beschreibt einen vollständig geladenen NPC-Bauplan.
 *
 * Diese Klasse beschreibt keinen echten NPC in der Welt.
 * Sie beschreibt nur, was aus den Resource-JSON-Dateien für eine RoleId geladen wurde.
 *
 * Beispiel:
 * RoleId lumberjack
 * -> HytaleRole
 * -> Display
 * -> Profile
 * -> Marker
 * -> Debug
 *
 * Wichtig:
 * Hier gibt es keine npcId.
 * Hier gibt es keine EntityRef.
 * Hier gibt es keine RuntimeNpc-Daten.
 * Hier gibt es keine state.json-Instanzdaten.
 */
public final class LoadedNpcDefinition {

}